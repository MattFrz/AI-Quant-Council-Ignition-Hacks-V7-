# Copy to .env.local
NEXT_PUBLIC_API_BASE=http://localhost:8000
# Phase 2: keep this true and render from data/fixtures/sample_trade_idea.json.
# Phase 3 step 3.8: flip to false and the frontend goes live. That is the whole
# integration, provided the Phase 1 contract held.
NEXT_PUBLIC_USE_FIXTURE=true

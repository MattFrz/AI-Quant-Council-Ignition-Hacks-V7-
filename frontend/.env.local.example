# Copy to .env.local
NEXT_PUBLIC_API_BASE=http://localhost:8000

# false = live backend (Phase 3 onward). true = local fixture, no backend needed.
# api.ts and stream.ts both read this, so D1 and D2 flip together.
NEXT_PUBLIC_USE_FIXTURE=false
